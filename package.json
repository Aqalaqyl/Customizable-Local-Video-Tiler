{
  "name": "customizable-local-video-tiler",
  "version": "0.1.0",
  "description": "A local Electron desktop app for organizing and playing downloaded videos in customizable tiles.",
  "private": true,
  "main": "src/main/main.js",
  "scripts": {
    "start": "electron .",
    "smoke": "ELECTRON_SMOKE_TEST=1 electron .",
    "check": "node --check src/main/main.js && node --check src/main/preload.js && node --check src/renderer/app.js"
  },
  "keywords": [
    "electron",
    "video",
    "tiling",
    "local"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "electron": "^42.4.0"
  }
}
